#include<bits/stdc++.h>
/*#include <iostream>
#include <cstring>*/
using namespace std;
// here strlen() is used
int main(){
    char s[]="Nitin";  //C-strings are simply implemented as a char array which is terminated by a null ...
     cout<<strlen(s);
    return 0;       //mentioned string as char array in  cpp  just like C and used strlen() because it is used in c
                              // as .length() is used in cpp
} 